VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional fallback if you don't want Supabase for the stream yet:
VITE_LIVE_STREAM_URL=

# QA only: disables 5 PM capture window, daily upload cap, stream age filter, and relaxes record min/max length.
# Prefer URL flag for ad-hoc testing (no redeploy): open the app with ?test=1
# VITE_APP_TEST_MODE=true

